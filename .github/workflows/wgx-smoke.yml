name: WGX Smoke

on:
  pull_request:
    branches: ["main"]
  push:
    branches: ["main"]

permissions:
  contents: read

jobs:
  smoke:
    uses: heimgewebe/wgx/.github/workflows/smoke.yml@main
    with:
      profile-path: .wgx/profile.yml

