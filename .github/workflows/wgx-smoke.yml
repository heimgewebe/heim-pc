name: wgx-smoke

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Checkout WGX Tooling
        uses: actions/checkout@v4
        with:
          repository: heimgewebe/wgx
          path: .wgx-tooling

      - name: Setup WGX
        run: |
          chmod +x .wgx-tooling/wgx
          echo "${PWD}/.wgx-tooling" >> $GITHUB_PATH

      - name: Run WGX Smoke
        run: wgx task smoke
