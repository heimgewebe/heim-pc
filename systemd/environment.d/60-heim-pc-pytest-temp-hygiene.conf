# Bound pytest's shared /tmp footprint while preserving failed-test evidence.
# Command-line -o overrides remain possible for an exceptional debugging run.
PYTEST_ADDOPTS="-o tmp_path_retention_count=1 -o tmp_path_retention_policy=failed"
