project:
  role: operator-entry
  canonical_entry: manifest/operator-entry.v1.json
  human_role: meaning_approval_abort
operator:
  kind: chatgpt_via_grabowski
  machine_first: true
heimgewebe:
  wgx:
    profile_expected: true
contracts:
  central_root: ../metarepo/contracts/heim-pc
ci:
  note: "CI validates the static entry contract and repository structure; live state requires fresh primary-source reads."
